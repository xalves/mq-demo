# Use official devcontainers Java image as base
FROM mcr.microsoft.com/devcontainers/java:0-21

# Install Maven (if not already present)
RUN apt-get update && apt-get install -y maven

# Install any additional tools here
